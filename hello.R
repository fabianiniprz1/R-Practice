# Ask users for name
name <- readline("What's your name? ")

# Concatenate the name with the phrase Hello, 
greeting <- paste("Hello, ", name, sep = "")

# it print in console the greeting variable
print(greeting)
